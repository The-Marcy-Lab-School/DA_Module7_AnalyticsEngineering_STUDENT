# Marts

**The final, BI-tool-ready tables that actually answer your domain's
real business questions** — the ones from your own Module 3
`SCENARIOS.md` entry. Built at a clearly stated grain (one row per
what?), denormalized appropriately for consumption (not a mirror of the
OLTP schema), and reused directly by your semantic metric(s).

This is the direct graduation of Module 3's own business questions —
you already answered several of these with ad-hoc SQL (and, for one
question, a materialized view). Now build the properly tested,
documented, layered version. Every mart-layer column needs a real
description in `schema.yml` — `documentation`'s own graded bar.

Nothing is given here — this is the actual project.
